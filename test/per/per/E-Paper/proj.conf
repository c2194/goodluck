# set(CONFIG_BFLOG 0) # BFLOG功能。
set(CONFIG_FREERTOS 1) # FreeRTOS操作系统。
set(CONFIG_TLSF 1) # TLSF内存管理器。
set(CONFIG_LWIP 1) # LWIP网络协议栈。
set(CONFIG_WIFI6 1) # WiFi6无线网络功能。
set(CONFIG_RF 1) # RF射频功能。
set(CONFIG_MBEDTLS 1) # mbedTLS加密库。
set(CONFIG_PSRAM 1) # PSRAM存储器
set(CONFIG_POSIX 1)
set(CONFIG_DHCPD 1) # DHCP服务器功能,用于自动分配IP地址和其他网络配置信息

# shell
set(CONFIG_SHELL 1)

# easyflash
set(CONFIG_PARTITION 1)
set(CONFIG_BFLB_MTD 1)
set(CONFIG_EASYFLASH4 1)

# set(CONFIG_MQTT 0) #  MQTT通信协议
# set(CONFIG_PING 0)

set(CONFIG_VSNPRINTF_FLOAT      1) #浮点数格式化
set(CONFIG_VSNPRINTF_FLOAT_EX   1) # 扩展浮点数格式化
set(CONFIG_VSNPRINTF_LONG_LONG  1) # 长长整数格式化功能

# Config
## mbedtls 加密库
# set(CONFIG_MBEDTLS_AES_USE_HW 0) # AES加密硬件加速
# set(CONFIG_MBEDTLS_BIGNUM_USE_HW 0) # 大数运算硬件加速
# set(CONFIG_MBEDTLS_ECC_USE_HW 0) # 椭圆曲线加密硬件加速
# set(CONFIG_MBEDTLS_SHA1_USE_HW 0) # SHA1哈希算法硬件加速
# set(CONFIG_MBEDTLS_SHA256_USE_HW 0) # SHA256哈希算法硬件加速
# set(CONFIG_MBEDTLS_SHA512_USE_HW 0) # SHA512哈希算法硬件加速

# wifi
set(CONFIG_VIF_MAX 2) # 最多支持2个虚拟接口
set(CONFIG_STA_MAX 4) # 最多支持4个站点连接
set(CONFIG_MAC_TXQ_DEPTH 32) # MAC发送队列的深度为32
set(CONFIG_MAC_RXQ_DEPTH 12) # MAC接收队列的深度为12